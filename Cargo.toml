[package]
name = "torven"
version = "0.5.0-dev"
edition = "2024"
rust-version = "1.86"
license = "MIT"
description = "Local-first LLM observability for macOS — track usage across Anthropic, OpenAI, Z.AI, and OpenRouter with AI-powered insights"

[lib]
name = "torven"
path = "src/lib.rs"

[[bin]]
name = "torven"
path = "src/bin/torven.rs"

[[bin]]
name = "torven-tui"
path = "src/bin/torven-tui.rs"

[dependencies]
tokio = { version = "1", features = ["rt", "macros", "time", "fs", "process", "signal"] }
reqwest = { version = "0.12", features = ["json", "rustls-tls"], default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
toml_edit = "0.22"
clap = { version = "4", features = ["derive", "env"] }
chrono = { version = "0.4", features = ["serde"] }
fs2 = "0.4"
tempfile = "3"
directories = "5"
thiserror = "2"
base64 = "0.22"
ratatui = "0.29"
crossterm = "0.28"

[dev-dependencies]
mockito = "1"
insta = { version = "1", features = ["json"] }
pretty_assertions = "1"
tempfile = "3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
