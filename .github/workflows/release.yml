name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      ref:
        description: "Tag or commit to build (e.g. v0.2.0)"
        required: true
        default: ""

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            artifact: torven-linux-x86_64
            builder: cargo
            os: ubuntu-22.04
            test: true
            needs_nasm: true
          - target: aarch64-unknown-linux-gnu
            artifact: torven-linux-aarch64
            builder: cross
            os: ubuntu-22.04
            test: false
            needs_nasm: false
          - target: aarch64-apple-darwin
            artifact: torven-macos-aarch64
            builder: cargo
            os: macos-14
            test: true
            needs_nasm: false

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref || github.ref }}

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install nasm (x86_64 native build; ring assembles its primitives)
        if: matrix.needs_nasm
        run: sudo apt-get update && sudo apt-get install -y nasm

      - name: Install cross (aarch64 cross-compile via Docker)
        if: matrix.builder == 'cross'
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Test native release build
        if: matrix.test
        run: cargo test --release --target ${{ matrix.target }}

      - name: Build release binaries
        run: ${{ matrix.builder }} build --release --target ${{ matrix.target }} --locked

      - name: Create tarball (both bins + LICENSE + config example)
        run: |
          STAGE=$(mktemp -d)
          cp target/${{ matrix.target }}/release/torven \
             target/${{ matrix.target }}/release/torven-tui \
             "$STAGE/"
          cp LICENSE README.md config.example.toml "$STAGE/"
          tar czf ${{ matrix.artifact }}.tar.gz -C "$STAGE" .
          rm -rf "$STAGE"

      - name: Compute SHA256
        run: |
          if command -v sha256sum >/dev/null 2>&1; then
            SHA=$(sha256sum ${{ matrix.artifact }}.tar.gz | awk '{print $1}')
          else
            SHA=$(shasum -a 256 ${{ matrix.artifact }}.tar.gz | awk '{print $1}')
          fi
          echo "$SHA  ${{ matrix.artifact }}.tar.gz" > ${{ matrix.artifact }}.tar.gz.sha256
          echo "Built ${{ matrix.artifact }}.tar.gz sha256=$SHA"

      - uses: actions/upload-artifact@v5
        with:
          name: ${{ matrix.artifact }}
          path: |
            ${{ matrix.artifact }}.tar.gz
            ${{ matrix.artifact }}.tar.gz.sha256

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: artifacts
          merge-multiple: true

      - name: Generate release body
        env:
          TAG: ${{ inputs.ref || github.ref_name }}
        run: |
          {
            echo "## Install"
            echo
            echo "### Arch (AUR)"
            echo
            echo '```bash'
            echo "yay -S torven-bin      # prebuilt binary from this release"
            echo "yay -S torven          # compiles from source"
            echo '```'
            echo
            echo "### Manual"
            echo
            echo '```bash'
            echo "mkdir -p ~/.local/bin"
            echo "curl -fsSL https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}/torven-linux-x86_64.tar.gz | tar xz -C ~/.local/bin torven torven-tui"
            echo
            echo "# macOS Apple Silicon"
            echo "curl -fsSL https://github.com/${GITHUB_REPOSITORY}/releases/download/${TAG}/torven-macos-aarch64.tar.gz | tar xz -C ~/.local/bin torven torven-tui"
            echo '```'
            echo
            echo "## Checksums (SHA256)"
            echo '```'
            cat artifacts/*.sha256
            echo '```'
          } > body.md

      - name: Create / update GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ inputs.ref || github.ref_name }}"
          # Replace any pre-existing assets so re-runs (workflow_dispatch on
          # an existing tag) overwrite cleanly.
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$TAG" artifacts/*.tar.gz artifacts/*.sha256 \
              --repo "$GITHUB_REPOSITORY" --clobber
            gh release edit "$TAG" --repo "$GITHUB_REPOSITORY" --notes-file body.md
          else
            gh release create "$TAG" artifacts/*.tar.gz artifacts/*.sha256 \
              --repo "$GITHUB_REPOSITORY" \
              --title "$TAG" \
              --notes-file body.md \
              --latest
          fi
