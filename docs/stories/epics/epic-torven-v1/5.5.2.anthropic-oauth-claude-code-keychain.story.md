# Story 5.5.2 — Anthropic OAuth via Claude Code Keychain + token expiry verify (ISSUE-D)

**Story ID:** 5.5.2.anthropic-oauth-claude-code-keychain
**Status:** Ready
**Epic:** Torven v1.0 — Pivot macOS native (`epic-torven-v1`)
**Wave:** 5.5 — Polish (carry-forwards Wave 5)
**Estimated Effort:** M (~1 dia — schema discovery spike + Rust creds reader extend + Swift probe Keychain + e2e bench)

---

## Story (User Story)

**As a** usuário do Torven que autentica via **Claude Code** (não Claude CLI standalone),
**I want** o Torven detectar e usar minhas credenciais Anthropic do Keychain `"Claude Code-credentials"` ao invés do path obsoleto `~/.claude/.credentials.json` (que só existe se eu uso o legacy Claude CLI),
**so that** o Settings UI mostre OAuth como "connected" corretamente E o Refresh button consiga fetch real-API Anthropic spend sem precisar instalar Claude CLI standalone separado.

---

## Contexto e Motivação

Story 5.2 implementou OAuth status probe via `FileManager.fileExists(atPath: ~/.claude/.credentials.json)`. Story 1.15+ e Story 4.x fetch usam o mesmo path. Mas:

- **Claude CLI standalone** (`brew install anthropic-ai/claude-cli/claude`): grava em `~/.claude/.credentials.json`
- **Claude Code** (`brew install --cask claude`): grava em macOS Keychain entry chamada `"Claude Code-credentials"`

Lorenzo usa **Claude Code** (confirmado bench 2026-06-07). Probe atual sempre retorna `.notConfigured` mesmo Lorenzo logado.

**Esse bug é maior que apenas status display:**
- AC-1 OAuth probe (Settings UI) — cosmético: false negative no badge
- AC-2 Fetch real API call: o `vendors/anthropic/fetch.rs::fetch_snapshot()` lê `creds_path: &Path` que aponta pro arquivo. Sem arquivo = sem fetch = sem dados Anthropic no chart.
- O bug significa que **toda a estória 1.15 (AI Insights baseline 0.90/1.00) só funcionou em testes mocados ou em máquinas com Claude CLI standalone instalado**, não no setup real do Lorenzo.

**Decisões cravadas:**
- **WAVE5.5-D5:** **Dual-source** — Keychain `"Claude Code-credentials"` é primary, file `~/.claude/.credentials.json` é fallback. Suporta usuários de ambos os fluxos. Decisão de "primary" via `try keychain first; fall back to file` ordering em Rust + Swift.
- **WAVE5.5-D6:** Story inclui spike pra **descobrir schema do Keychain `"Claude Code-credentials"`**. Sem schema sabido, @dev não consegue parse. AC-0 abaixo.
- **WAVE5.5-D7:** **Token expiry verify real** entra junto — atual probe (file-exists) ignora se o token está expirado. Status probe nova deve retornar `.connected` apenas se token estiver válido (não-expirado).
- **WAVE5.5-D8:** Update CLAUDE.md secret-discipline rule (lines 80-87 atual) — Keychain queries devem ser tratadas como leituras de credenciais OAuth com mesmo cuidado de `jq 'keys'` files: NÃO logar conteúdo dos blobs, apenas chaves/metadata.
- **WAVE5.5-D9:** Story NÃO toca em OpenAI Codex CLI path (`~/.codex/auth.json`) — file-based, funcionando, fora de escopo.

---

## Acceptance Criteria

### Phase 0 — Schema discovery spike (BLOCKING)

**AC-0:** `docs/architecture/spikes/claude-code-keychain-schema.md` criado documentando:
- Como inspecionar Keychain entry `"Claude Code-credentials"` (`security find-generic-password -s "Claude Code-credentials" -w` retorna password blob)
- Estrutura JSON observada (com Lorenzo executando comando — captura sem expor token literal)
- Mapeamento de fields pra `ClaudeAiOauth` struct existente em `crates/torven-core/src/vendors/anthropic/creds.rs`
- Decisão **GO** (schema compatível, parse direto) ou **NEEDS-ADAPT** (schema diferente, precisa adapter)

### Phase 1 — Rust creds source dual

**AC-1:** `crates/torven-core/src/vendors/anthropic/creds.rs` ganha enum `CredsSource`:
```rust
pub enum CredsSource {
    File(PathBuf),                       // legacy ~/.claude/.credentials.json
    Keychain(String),                    // service name "Claude Code-credentials"
}

pub fn read_from_source(source: &CredsSource) -> Result<ClaudeAiOauth> {
    match source {
        CredsSource::File(p) => read_from(p),
        CredsSource::Keychain(svc) => read_from_keychain(svc),
    }
}

pub fn write_back_to_source(source: &CredsSource, oauth: &ClaudeAiOauth) -> Result<()> { ... }
```

**AC-2:** `read_from_keychain(service)` usa `keyring` crate (já workspace dep da Story 1.13/Wave 5) pra ler Keychain entry, deserializar como JSON, mapear pra `ClaudeAiOauth`.

**AC-3:** `write_back_to_keychain(service, oauth)` simétrico — usado após `oauth::refresh` pra persistir novo access_token + refresh_token + expires_at.

**AC-4:** `fetch_snapshot()` (`fetch.rs:56`) signature muda de `creds_path: &Path` pra `creds_source: &CredsSource`. Callers atualizados.

**AC-5:** Default resolver function `default_anthropic_creds_source()` em `creds.rs`:
```rust
pub fn default_anthropic_creds_source() -> CredsSource {
    // Try Keychain first (Claude Code); fall back to file (legacy Claude CLI)
    if read_from_keychain("Claude Code-credentials").is_ok() {
        CredsSource::Keychain("Claude Code-credentials".into())
    } else {
        CredsSource::File(default_creds_file_path())
    }
}
```

**AC-6:** Unit tests em `creds.rs`:
- `read_from_keychain_parses_valid_blob` (fixture inline) — pelo menos 1 happy path
- `read_from_keychain_returns_storage_err_when_missing` — fixture com service inexistente
- `default_anthropic_creds_source_prefers_keychain_when_present` (mock dual presence)
- `default_anthropic_creds_source_falls_back_to_file_when_keychain_missing`

### Phase 2 — Swift probe via Keychain

**AC-7:** Nova UDL FFI surface `ffi_anthropic_oauth_status() -> OAuthStatusFfi`:
```
[Async]
OAuthStatusFfi ffi_anthropic_oauth_status();

dictionary OAuthStatusFfi {
    boolean is_connected;
    boolean is_expired;
    optional i64 expires_at_secs;
    string source;     // "claude_code_keychain" | "claude_cli_file" | "none"
};
```

**AC-8:** Implementação Rust:
1. `default_anthropic_creds_source()` → if `Keychain` source, read + check `expires_at_ms > now() → is_connected=true, is_expired=false`
2. If `File` source idem
3. If neither → `is_connected=false`

**AC-9:** `SettingsViewModel.swift:113-116` `refreshOAuthStatus()` Anthropic path:
- Remove `probe(relativePath: "/.claude/.credentials.json")`
- Add `let status = try? await TorvenCore.ffiAnthropicOauthStatus()`
- Map `status.is_connected && !status.is_expired` → `.connected`, else `.notConfigured`
- Mantém `openaiStatus` inalterado (file-based, fora de escopo)

**AC-10:** `OAuthStatus` enum (em SettingsViewModel.swift) ganha info extra opcional pra debug:
```swift
enum OAuthStatus: Equatable {
    case connected(expiresAt: Date?)
    case notConfigured
    case expired   // new — token presente mas expirado
}
```
View layer renderiza:
- `.connected`: green checkmark + "Connected" (opcional tooltip com expiry date)
- `.expired`: orange exclamation + "Expired — re-run Claude Code login"
- `.notConfigured`: grey + "Not configured"

**AC-11:** `apple/Torven/Settings/VendorSettingsRow.swift` (ou similar — onde o badge Anthropic é renderizado) atualiza pra novo enum case `.expired`.

### Phase 3 — CLAUDE.md update

**AC-12:** `CLAUDE.md` "Secret-discipline rules" section ganha:
> - **Never query Keychain entries blindly** for OAuth credentials. Use
>   `security find-generic-password -s "Claude Code-credentials"` to confirm
>   existence (returns metadata only), and `-w` flag (which prints the
>   password) **only inside a controlled test fixture**, never in a regular
>   investigation. Like JSON OAuth files, treat the Keychain blob as opaque.

### Phase 4 — E2E bench

**AC-13:** Lorenzo bench:
1. Confirma logado no Claude Code (`security find-generic-password -s "Claude Code-credentials"` retorna data)
2. Abre Torven → Settings → Anthropic row mostra **"Connected"** (green) — não "Not configured"
3. Click Refresh button (⌘R) → real-API Anthropic fetch executa, snapshot grava em SQLite
4. Chart atualiza com Anthropic spend visível (não só OpenRouter)

**AC-14:** Test fallback path (mock):
- Remover temporarily Keychain entry (renomear svc OR `security delete-generic-password`)
- Garantir que probe **não** crash; retorna `.notConfigured` graciosamente
- Re-adicionar Keychain entry
- Probe volta `.connected`
- Documenta no Dev Agent Record

---

## Tasks / Subtasks

- [ ] **Phase 0 — AC-0: Schema spike**
  - [ ] Lorenzo bench: `security find-generic-password -s "Claude Code-credentials" -w | jq 'keys'` (extract structure sem expor secrets)
  - [ ] Document em `docs/architecture/spikes/claude-code-keychain-schema.md`
  - [ ] Verdict: GO ou NEEDS-ADAPT
- [ ] **Phase 1 — Rust creds source dual (AC-1 → AC-6)**
  - [ ] Add enum `CredsSource` em `creds.rs`
  - [ ] Implementar `read_from_keychain` + `write_back_to_keychain` (use `keyring` crate)
  - [ ] Refactor `fetch_snapshot` pra aceitar `&CredsSource`
  - [ ] Update callers (`uniffi_exports.rs` + qualquer caller upstream)
  - [ ] `default_anthropic_creds_source()` resolver function
  - [ ] 4 unit tests novos
- [ ] **Phase 2 — Swift probe via Keychain (AC-7 → AC-11)**
  - [ ] Add UDL `ffi_anthropic_oauth_status() -> OAuthStatusFfi`
  - [ ] Rust impl em `uniffi_exports.rs`
  - [ ] Regen Swift bindings (automático em build)
  - [ ] Update `OAuthStatus` enum (adicionar `.expired`)
  - [ ] Update `SettingsViewModel.refreshOAuthStatus()` Anthropic path
  - [ ] Update View renderer pra `.expired` case
- [ ] **Phase 3 — CLAUDE.md (AC-12)**
  - [ ] Append secret-discipline rule sobre Keychain
- [ ] **Phase 4 — Bench (AC-13, AC-14)**
  - [ ] Lorenzo bench: e2e Anthropic real-API fetch via Keychain
  - [ ] Lorenzo bench: fallback path mock-deletando Keychain entry
  - [ ] Documentar no Dev Agent Record

---

## Dev Notes

### Constraints técnicas

- **`keyring` crate cross-platform:** já workspace dep (Story 1.13 wave 5). Mas macOS Keychain access pode requerer `Security.framework` linkage ou entitlement. Verificar Story 1.13's keychain_blob.rs pra padrões.
- **Schema unknown até AC-0:** essa é a maior incógnita. Provavelmente `{ "claudeAiOauth": { "accessToken": "...", "refreshToken": "...", "expiresAt": ..., ... } }` mirroring file format — mas confirmar antes de codar. Spike é BLOCKING — se NEEDS-ADAPT, scope da story aumenta.
- **`security` CLI vs Keychain API:** `security` é CLI shell tool, mas Rust app vai usar `keyring` crate (que internamente usa Security.framework). Esperar comportamento equivalente; if discrepâncias, troubleshoot via Apple docs.
- **Async FFI:** `ffiAnthropicOauthStatus` é `[Async]` UDL pra não bloquear UI thread durante Keychain query. AR-2 cancel pattern aplica.
- **Token expiry check unit:** `expires_at_ms` em ClaudeAiOauth é ms epoch. `now()` deve usar mesmo unit. Story 1.15+ tem `oauth::needs_refresh` pattern — re-use.

### Relevant Source Tree

```
crates/torven-core/src/
├── vendors/anthropic/
│   ├── creds.rs                       ← edit AC-1, AC-2, AC-3, AC-5, AC-6
│   ├── fetch.rs                       ← edit AC-4 (signature change)
│   ├── oauth.rs                       ← may need update if Keychain refresh writeback differs
│   └── mod.rs                         ← re-exports update
├── torven_core.udl                    ← edit AC-7 (new FFI surface)
└── uniffi_exports.rs                  ← edit AC-7 (new export)

apple/Torven/Settings/
├── SettingsViewModel.swift            ← edit AC-9, AC-10
├── SettingsView.swift                 ← edit AC-11 (renderer for .expired)
└── VendorKeyRow.swift                 ← possible edit if VendorSettingsRow shared

docs/architecture/spikes/
└── claude-code-keychain-schema.md     ← NEW (AC-0)

CLAUDE.md                              ← edit AC-12
```

### Cross-references

- Story 5.2: established Keychain blob FFI pattern (`ffi_keychain_get_blob` / `ffi_keychain_set_blob`). Story 5.5.2 estende com OAuth-specific reader.
- Story 5.2.1 hotfix: identificou ISSUE-D durante bench. Path errado documentado em session_checkpoint.
- Story 1.15+ (AI Insights): fetcher Anthropic atualmente quebrado pra Claude Code users. Esta story conserta.
- Story 1.13 (keychain_blob.rs): keyring crate setup base.
- WAVE5-D7 (Wave 5 decision): OpenAI = Codex OAuth-only. Esta story confirma simetria pra Anthropic.

### Riscos identificados

| ID | Risco | Mitigação |
|---|---|---|
| R5.5.2-1 | Schema Keychain Claude Code muda formato em update do app | Story usa best-effort parse + fallback graceful pra `.notConfigured` em parse fail |
| R5.5.2-2 | Refresh token writeback Keychain falha (permission issues) | `oauth::refresh` writeback é best-effort já no current code; manter pattern |
| R5.5.2-3 | Schema discovery (AC-0) revela JSON shape MUITO diferente do `ClaudeAiOauth` | Spike output guide adapter pattern; scope cresce mas previsível |
| R5.5.2-4 | Concurrent access Keychain (Claude Code rodando + Torven querying) | Keychain é mutex-safe por design (sem flock necessário) |
| R5.5.2-5 | Sandboxed apps não podem ler Keychain entries de outros apps | Torven não é sandboxed (decisão Wave 7.1 / WAVE7-D3) — OK |
| R5.5.2-6 | Lorenzo logado mas Claude Code só renovou token recentemente; expires_at em segundos vs ms confunde | Test em creds.rs valida unit (`expires_at_ms` vs `expires_at_secs`) |

### Anti-scope (NÃO fazer nesta story)

- ❌ OpenAI Codex Keychain migration — fora de escopo (file-based funciona)
- ❌ Anthropic API key support (não-OAuth) — fora de escopo v1.0 (WAVE5-D7)
- ❌ Claude CLI standalone fallback removal — manter retrocompat
- ❌ UI pra "Login with Claude Code" button — out of scope (user já loga via Claude Code app)
- ❌ Auto-refresh Keychain entry em background scheduler — Wave 5.5+ ou v1.0.1
- ❌ Migration tooling pra mover credentials file → Keychain — fora de escopo
- ❌ ChartViewModel refresh logic mudança — só `fetch_snapshot` signature

### Testing

- **Rust unit tests (AC-6):** 4 testes novos em `creds.rs`. Mock keyring entries via test fixtures (use `mockall` ou `keyring`'s test mode).
- **No XCTest yet (TEST-001 = Story 5.5.3):** Swift validation é manual smoke (AC-13, AC-14). XCTest target deve estar pronto pré-5.5.3, então 5.5.3 retroativamente adiciona tests pra `SettingsViewModel.refreshOAuthStatus()`.
- **CI:** `cargo test --workspace` deve incluir os 4 testes novos verde.
- **Bench obrigatório:** AC-13 e AC-14 — sem isso, claim "ISSUE-D resolvido" não é verificável.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-06-07 | 0.1 | Story drafted by @sm (Wave 5.5 pivot pós-Wave 7 defer) | @sm (River) |
| 2026-06-07 | 0.2 | Validation 10-point checklist: **GO (10/10)**. Status: Draft → Ready. Caveat #6 documentado: AC-0 spike é BLOCKING — Lorenzo bench obrigatório (`security find-generic-password -s "Claude Code-credentials" -w \| jq 'keys'`) antes de @dev `*develop 5.5.2`. Se schema NEEDS-ADAPT, escalar scope antes de implementar. | @po (Pax) |
