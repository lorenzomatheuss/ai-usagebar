# Story 1.4 — Migrate format.rs + Extrair torven-core lib (mover vendors e módulos core)

**Story ID:** 1.4.extract-torven-core
**Status:** InReview
**Epic:** Torven v1.0 — Pivot macOS native
**Estimated Effort:** M (1 dia)

---

## Story (User Story)

Como desenvolvedor do Torven, eu quero mover todo o código reaproveitável de `src/` para `crates/torven-core/src/`, migrar `format.rs` de saída Pango para `RawMetrics` cross-platform, e garantir que o crate `torven-tui` ainda builda corretamente dependendo do `torven-core`, para que o workspace Cargo esteja na estrutura final com separação limpa entre core e UI.

---

## Contexto e Motivação

Após a Story 1.3 (cleanup), `src/` contém apenas os módulos válidos. Esta story move tudo para `crates/torven-core/src/` e faz a migração de `format.rs` de strings Pango para structs semânticas.

Dois movimentos principais:

1. **Física do código:** mover `src/{anthropic,openai,...,cache,countdown,error,pacing,usage,vendor,config,format,tui}` → `crates/torven-core/src/vendors/` e `crates/torven-core/src/`
2. **Refactor de format.rs:** substituir `FormattedSnapshot` com markup Pango por `RawMetrics { cost_usd, pct_used, tokens_used, label_kind }` — Swift formata a string final; TUI tem helper próprio

**Referência:** `docs/architecture/preservation-map.md#migrate` e `docs/architecture/torven-v1-adr.md#4-cleanup-plan`.

---

## Acceptance Criteria

**AC-1:** Todos os módulos vendor (`anthropic`, `openai`, `openrouter`, `zai`, `gemini`) existem em `crates/torven-core/src/vendors/` com imports atualizados para o novo caminho.

**AC-2:** Módulos core (`cache.rs`, `countdown.rs`, `error.rs`, `pacing.rs`, `usage.rs`, `vendor.rs`) existem em `crates/torven-core/src/`.

**AC-3:** `config.rs` existe em `crates/torven-core/src/` (mover sem alterar conteúdo — o refactor de conteúdo é na Story 1.6).

**AC-4:** `format.rs` em `crates/torven-core/src/` retorna `RawMetrics` (sem qualquer string Pango, sem markup HTML):
```rust
pub struct RawMetrics {
    pub cost_usd: Option<f64>,
    pub pct_used: Option<f64>,
    pub tokens_used: Option<u64>,
    pub label_kind: LabelKind,
}

pub enum LabelKind {
    PercentOfWindow,
    MessagesQuota,
    UsdSpent,
    OAuthUnlinked,
}
```

**AC-5:** `crates/torven-tui/` contém `src/format_tui.rs` com helpers de formatação específicos do TUI (string Pango/ratatui). O TUI ainda builda e funciona corretamente.

**AC-6:** `src/tui/` movido para `crates/torven-tui/src/` com imports atualizados para `torven_core::*`.

**AC-7:** `cargo build --workspace` completa sem erros. `cargo test --workspace` (todos os testes existentes) passam.

**AC-8:** `src/` original fica vazio ou removido — todo o código migrado. `Cargo.toml` raiz continua como workspace manifest sem crate próprio.

---

## Tasks / Subtasks

- [x] **T1 — Criar estrutura de diretórios em torven-core**
  ```
  crates/torven-core/src/vendors/
  crates/torven-core/src/vendors/anthropic/
  crates/torven-core/src/vendors/openai/
  crates/torven-core/src/vendors/openrouter/
  crates/torven-core/src/vendors/zai/
  ```
  Note: `gemini/` subdir not created — no `src/gemini/` module exists in
  the source tree (story brief listed it as forward-looking; per Article IV
  "No Invention" we don't manufacture an empty crate today).

- [x] **T2 — Mover vendor modules**
  - `git mv src/anthropic/{mod,creds,fetch,oauth,types}.rs` → `crates/torven-core/src/vendors/anthropic/`
  - `git mv src/openai/{mod,creds,fetch,oauth,types}.rs` → `crates/torven-core/src/vendors/openai/`
  - `git mv src/openrouter/{mod,fetch,types}.rs` → `crates/torven-core/src/vendors/openrouter/`
  - `git mv src/zai/{mod,fetch,types}.rs` → `crates/torven-core/src/vendors/zai/`
  - Per-vendor Waybar renderers deleted (see "Renderer cleanup" below).
  - Created `crates/torven-core/src/vendors/mod.rs` with `From<FetchOutcome>`
    impls for each vendor (replaces the deleted `From` impls that lived in
    the dropped `vendor.rs` renderers).

- [x] **T3 — Mover módulos core (PRESERVE)**
  - `git mv src/{cache,countdown,error,pacing,usage,vendor,config}.rs` → `crates/torven-core/src/`
  - `src/vendor.rs` was stripped of the deleted `widget::cli::Cli` import
    and its `RenderOpts` struct (Waybar-specific; consumers now use
    `RawMetrics` directly). Cross-platform pieces (`VendorId`,
    `VendorOutcome`, `HTTP_CLIENT_TIMEOUT`) preserved.
  - `usage.rs::anthropic_severity` rewired from `pango::severity_for` to
    new `pacing::severity_for` (same thresholds: 50/75/90).

- [x] **T4 — Migrar `format.rs` para `RawMetrics` (MIGRATE)**
  - Deleted legacy `src/format.rs` (it was a placeholder-substitution
    utility — `pango.rs` had the markup, and it's already gone in 1.3).
  - Created `crates/torven-core/src/format.rs` with `RawMetrics`,
    `LabelKind`, and `compute_metrics(snapshot) -> RawMetrics` projecting
    each `VendorSnapshot` variant onto the new semantic surface.
  - `lib.rs` re-exports `RawMetrics`, `LabelKind`, `compute_metrics`.

- [x] **T5 — Criar `crates/torven-tui/src/format_tui.rs`**
  - Defines `calm/amber/critical` ratatui colors (75 / 90 thresholds for
    the TUI; the menu-bar label uses the legacy 50/75/90 thresholds via
    `pacing::severity_for`).
  - `format_for_tui(&RawMetrics) -> String` returns a short headline
    string per `LabelKind`.
  - `color_for_severity(PaceSeverity)` replaces the deleted
    `pango::severity_color` helper used by `panels::render_metric`.

- [x] **T6 — Mover TUI para `crates/torven-tui/src/`**
  - `git mv src/tui/{app,panels,settings,view,mod}.rs` →
    `crates/torven-tui/src/`. Renamed `mod.rs` to `lib.rs` so the crate
    has a library surface (so the integration tests and `main.rs` share
    the same module graph).
  - `git mv src/bin/torven-tui.rs` → `crates/torven-tui/src/main.rs`.
  - Imports rewritten: `crate::theme::Theme` is gone (palette constants in
    `view::palette`); `crate::pango/waybar/tooltip` references replaced
    with `format_tui` calls; `crate::*` cross-crate types replaced with
    `torven_core::*`.

- [x] **T7 — Atualizar `crates/torven-core/src/lib.rs`**
  - Module tree: `vendors`, `cache`, `config`, `countdown`, `error`,
    `format`, `pacing`, `usage`, `vendor`, `uniffi_exports`.
  - Re-exports `AppError`/`Result` and the new format surface.
  - `uniffi::include_scaffolding!` retained at crate root per Story 1.2.

- [x] **T8 — Atualizar `crates/torven-core/Cargo.toml`**
  - Dependencies kept: `tokio`, `reqwest`, `serde`, `serde_json`, `toml`,
    `clap`, `chrono`, `fs2`, `tempfile`, `directories`, `thiserror`,
    `base64`, `uniffi`.
  - `toml_edit` removed — only consumer (`settings.rs`) moved to the TUI
    crate, which now lists it.
  - `[package.metadata.cargo-machete].ignored` shrunk from 13 entries to
    `[]` — every bootstrap stub from Story 1.1 ended up consumed by this
    story. Comment explains the empty list is intentional and forward-
    looking for stories 1.13/1.15/1.20.
  - `crates/torven-tui/Cargo.toml` gained `tokio`, `reqwest`, `chrono`,
    `toml_edit`, `directories`; removed `ratatui`/`crossterm` from
    `cargo-machete` ignored list.

- [x] **T9 — Limpar `src/` antigo**
  - `git rm` on every remaining file under `src/`. Directory is gone.
  - Workspace root `Cargo.toml` already contains only `[workspace]` (no
    `[package]`) — confirmed.
  - `tests/` directory moved to `crates/torven-core/tests/` (mockito E2E
    + live smoke). `anthropic_e2e.rs` rewritten to assert on snapshot
    fields + `RawMetrics` (the previous version asserted on Waybar JSON
    snapshots produced by deleted modules).

- [x] **T10 — Rodar `cargo build --workspace` e resolver erros**
  - `cargo build --workspace` — green
  - `cargo test --workspace` — 150 tests pass (118 torven-core lib +
    3 anthropic_e2e + 29 torven-tui lib + 4 ignored live smoke)
  - `cargo clippy --workspace --all-targets -- -D warnings` — clean
  - `cargo machete` — clean (zero unused deps, zero false-positive
    ignored entries)

### Renderer cleanup (out of original scope, required to compile)

Story 1.3's cleanup left orphaned per-vendor renderers that still imported
the deleted `pango`/`theme`/`tooltip`/`waybar`/`widget` modules:

- `src/openai/vendor.rs` (Waybar Pango renderer)
- `src/openrouter/vendor.rs` (Waybar Pango renderer)
- `src/zai/vendor.rs` (Waybar Pango renderer)

These files could not compile under the current crate after Story 1.3 (the
old top-level `src/lib.rs` was non-functional / not a workspace member,
which masked the failure). Story 1.4 deletes them as part of the
migration — the cross-platform rendering is now Swift's job, with the TUI
covered by `format_tui` + `panels.rs`. Traces to AC-7 (workspace build +
test green is non-negotiable).

Similarly, the TUI files (`app.rs`, `view.rs`, `panels.rs`, `settings.rs`)
imported `crate::theme::Theme` and `crate::waybar::request_refresh` — both
deleted in Story 1.3. Refactored to remove the Theme parameter (fixed
palette via `view::palette`) and drop the SIGRTMIN signaling (macOS
menu-bar app picks up config changes on its next refresh tick).

---

## Dev Notes

### Architecture References

- **Preservation Map MIGRATE** (`docs/architecture/preservation-map.md#migrate`): `format.rs` migra de Pango para `RawMetrics`. Swift formata a string/cor final. TUI tem helper separado.
- **Preservation Map PRESERVE** (`docs/architecture/preservation-map.md#preserve`): vendor modules e core movem sem mudança funcional.
- **ADR-1 cleanup plan** (`docs/architecture/torven-v1-adr.md#4-cleanup-plan`): `src/{anthropic,openai,gemini,zai,openrouter}/*` → `crates/torven-core/src/vendors/{vendor}/`
- **ADR-1** nota importante: `torven-app` crate Cargo NÃO existe — o app Swift vive no Xcode project. Workspace tem apenas `torven-core` e `torven-tui`.

### Mudança Crítica em format.rs

O `format.rs` atual gera strings Pango com markup:
```rust
// ANTES (Waybar):
pub fn format_cost(usd: f64) -> String {
    format!("<span color='#ff0000'>${:.2}</span>", usd)  // Pango markup — DELETE
}
```

Após a migração:
```rust
// DEPOIS (cross-platform):
pub fn compute_metrics(snapshot: &VendorSnapshot) -> RawMetrics {
    RawMetrics {
        cost_usd: snapshot.cost_usd,
        pct_used: snapshot.pct_used,
        label_kind: LabelKind::UsdSpent,
        // ...
    }
}
```

O Swift decide a cor (via `Color.calm`, `Color.amber`, `Color.critical` em Assets.xcassets). O TUI decide o markup ratatui via `format_tui.rs`.

### Files Affected

**MOVER (de src/ para crates/torven-core/src/):**
- `src/anthropic/` → `crates/torven-core/src/vendors/anthropic/`
- `src/openai/` → `crates/torven-core/src/vendors/openai/`
- `src/openrouter/` → `crates/torven-core/src/vendors/openrouter/`
- `src/zai/` → `crates/torven-core/src/vendors/zai/`
- `src/gemini/` → `crates/torven-core/src/vendors/gemini/`
- `src/{cache,countdown,error,pacing,usage,vendor,config}.rs` → `crates/torven-core/src/`

**MIGRAR (com mudança de conteúdo):**
- `src/format.rs` → `crates/torven-core/src/format.rs` (Pango → RawMetrics)

**MOVER para torven-tui:**
- `src/tui/` → `crates/torven-tui/src/`
- `src/bin/torven-tui.rs` → `crates/torven-tui/src/main.rs`

**CRIAR:**
- `crates/torven-tui/src/format_tui.rs`
- `crates/torven-core/src/vendors/mod.rs`

**DELETAR:**
- `src/` após migração bem-sucedida

### Dependências

- Story 1.3 deve estar completa (código morto já deletado)
- Story 1.1 deve estar completa (estrutura de crates existindo)
- Story 1.2 pode estar em paralelo (não conflita com mover os módulos)

### Nota sobre `tests/`

Os testes em `tests/anthropic_e2e.rs`, `tests/live.rs`, etc. precisam ser movidos ou ter paths atualizados. O movimento é para `crates/torven-core/tests/`. Manter `make smoke` funcional.

### Testing Strategy

- `cargo build --workspace` deve compilar completamente
- `cargo test --workspace` deve rodar todos os testes existentes e passar
- `cargo clippy --all-targets -- -D warnings` sem warnings
- `make smoke` (live tests) deve continuar funcional

### Definition of Done

- [x] `src/` removido (todo código em `crates/`)
- [x] `cargo build --workspace` verde
- [x] `cargo test --workspace` verde — 150 tests pass, zero regressions
- [x] `format.rs` retorna `RawMetrics` sem Pango/HTML
- [x] `crates/torven-tui/` builda; lib + bin verdes; 29 tui tests pass

---

## File List

### MOVED (git mv — history preserved)

| Old | New |
|-----|-----|
| `src/anthropic/{mod,creds,fetch,oauth,types}.rs` | `crates/torven-core/src/vendors/anthropic/*` |
| `src/openai/{mod,creds,fetch,oauth,types}.rs` | `crates/torven-core/src/vendors/openai/*` |
| `src/openrouter/{mod,fetch,types}.rs` | `crates/torven-core/src/vendors/openrouter/*` |
| `src/zai/{mod,fetch,types}.rs` | `crates/torven-core/src/vendors/zai/*` |
| `src/cache.rs` | `crates/torven-core/src/cache.rs` |
| `src/countdown.rs` | `crates/torven-core/src/countdown.rs` |
| `src/error.rs` | `crates/torven-core/src/error.rs` |
| `src/pacing.rs` | `crates/torven-core/src/pacing.rs` |
| `src/usage.rs` | `crates/torven-core/src/usage.rs` |
| `src/vendor.rs` | `crates/torven-core/src/vendor.rs` |
| `src/config.rs` | `crates/torven-core/src/config.rs` |
| `src/tui/app.rs` | `crates/torven-tui/src/app.rs` |
| `src/tui/panels.rs` | `crates/torven-tui/src/panels.rs` |
| `src/tui/settings.rs` | `crates/torven-tui/src/settings.rs` |
| `src/tui/view.rs` | `crates/torven-tui/src/view.rs` |
| `src/tui/mod.rs` | `crates/torven-tui/src/lib.rs` (renamed to library entry) |
| `src/bin/torven-tui.rs` | `crates/torven-tui/src/main.rs` |
| `tests/anthropic_e2e.rs` | `crates/torven-core/tests/anthropic_e2e.rs` (rewritten) |
| `tests/live.rs` | `crates/torven-core/tests/live.rs` |
| `tests/fixtures/*.json` | `crates/torven-core/tests/fixtures/` |

### CREATED

- `crates/torven-core/src/format.rs` — `RawMetrics`, `LabelKind`, `compute_metrics()`
- `crates/torven-core/src/vendors/mod.rs` — vendor module re-exports + `From<FetchOutcome>` impls
- `crates/torven-tui/src/format_tui.rs` — `format_for_tui()`, color helpers

### MODIFIED (existing files updated in place)

- `crates/torven-core/src/lib.rs` — added every migrated module; re-exports `RawMetrics`/`LabelKind`/`compute_metrics`
- `crates/torven-core/src/vendor.rs` — dropped `widget::cli::Cli` import + `RenderOpts` struct
- `crates/torven-core/src/usage.rs` — `anthropic_severity` rewired to `pacing::severity_for`
- `crates/torven-core/src/pacing.rs` — added `severity_for(pct)` (replaces deleted `pango::severity_for`)
- `crates/torven-core/src/vendors/openai/oauth.rs` — `crate::anthropic::*` → `crate::vendors::anthropic::*`
- `crates/torven-core/src/vendors/anthropic/creds.rs` — `read_from` now returns `Credentials` for `ENOENT` (was `Io`); makes the existing test pass and matches the "actionable" intent
- `crates/torven-core/Cargo.toml` — removed `toml_edit`; trimmed cargo-machete ignored list to empty
- `crates/torven-tui/Cargo.toml` — added `tokio`/`reqwest`/`chrono`/`toml_edit`/`directories`; library + binary surface
- `crates/torven-tui/src/main.rs` — switched to `torven_core::*` + `torven_tui::*` imports
- `crates/torven-tui/src/app.rs` — dropped `Theme` field; `crate::*` → `torven_core::*`
- `crates/torven-tui/src/view.rs` — fixed palette via `palette` submodule; `crate::theme` gone; settings::render call no longer passes theme
- `crates/torven-tui/src/panels.rs` — `crate::pango::severity_for` → `pacing::severity_for`; render fns no longer take `theme`; uses `format_tui::color_for_severity`
- `crates/torven-tui/src/settings.rs` — dropped `Theme`, dropped `waybar::request_refresh`; `cache::atomic_write` → `torven_core::cache::atomic_write`

### DELETED

- `src/openai/vendor.rs`, `src/openrouter/vendor.rs`, `src/zai/vendor.rs` (Waybar Pango renderers, dead code post-Story 1.3)
- `src/format.rs` (legacy placeholder-substitution utility; the actual Pango layer was already gone)
- `src/lib.rs` (top-level orphan that wasn't a workspace member)
- `src/` entire directory (after the above moves/deletes)

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-05-31 | @sm (River) | Story criada — Draft |
| 2026-05-31 | @po (Pax) | Status: Draft → Ready (validation GO, score 8/10) |
| 2026-05-31 | @dev (Dex) | T1-T3: moved every vendor + core module via `git mv` (history preserved) |
| 2026-05-31 | @dev (Dex) | T4-T5: built RawMetrics + format_tui (Pango layer fully gone) |
| 2026-05-31 | @dev (Dex) | T6-T7: TUI migrated; library surface added; cross-crate imports rewired |
| 2026-05-31 | @dev (Dex) | T8: cargo-machete ignored list shrunk 13 → 0; toml_edit moved to TUI |
| 2026-05-31 | @dev (Dex) | T9: `src/` deleted; tests moved to `crates/torven-core/tests/` |
| 2026-05-31 | @dev (Dex) | T10: build/test/clippy/machete all green (150 tests pass) — Status: InProgress → InReview |
