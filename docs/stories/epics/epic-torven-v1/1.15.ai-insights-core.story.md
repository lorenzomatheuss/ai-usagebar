# Story 1.15 — AI Insights core: Anthropic client + tool_use + streaming callback

**Story ID:** 1.15.ai-insights-core
**Status:** InReview
**Epic:** Torven v1.0 — Pivot macOS native
**Estimated Effort:** L (1.5-2 dias, incluindo spike async FFI 0.5d)

---

## Story (User Story)

Como desenvolvedor do Torven, eu quero o módulo `insights` em `torven-core` com cliente Anthropic Messages API operando em `tool_use` mode com streaming, trait `LlmClient` com implementação real e mock, `prompts/insights.v1.md` com o primeiro prompt versionado, budget guards de custo/latência, e callback interface FFI para streaming de tokens para Swift, para que o backend de AI Insights esteja pronto para consumo pelo SwiftUI (Story 1.19 do Grupo 2).

---

## Contexto e Motivação

FR-7 do PRD é o diferenciador central do Torven: AI Insights com eval pipeline público, prompts versionados e structured outputs. Esta story entrega o backend inteiro em Rust — o lado Swift (InsightsPanel.swift) vem na Story 1.19 do Grupo 2.

Elementos críticos:
1. **Anthropic tool_use mode** — mais robusto que prompted JSON para structured outputs em Claude 3.5+ (ADR-7)
2. **Streaming via callback interface UniFFI** — tokens chegam token a token para o Swift (AR-2 spike obrigatório)
3. **tokio runtime como `OnceCell`** — shared com FFI context, single-thread para macOS sandbox safety
4. **Privacy redaction** — payload enviado para Anthropic nunca contém api_keys nem account names

**Referência:** `docs/architecture/torven-v1-adr.md#adr-7` (tool_use mode) e `docs/prd/torven-v1.md#6-ai-insights`.

---

## Acceptance Criteria

**AC-1:** Prompt inicial em `prompts/insights.v1.md` com frontmatter:
```yaml
---
version: v1
created_at: 2026-05-31
author: "@pm + @sm"
superseded_by: null
eval_baseline_score: null  # preenchido após eval runner (Story 1.17)
---
```
E body com placeholders `{{usage_payload}}` e `{{user_context}}`, estruturado para retornar o schema JSON via tool_use.

**AC-2:** Schema JSON do tool em `prompts/insights.v1.schema.json` com os campos especificados no PRD §6.3:
```json
{
  "headline": "string (max 120 chars)",
  "insights": [{"type", "severity", "message", "evidence"}],
  "recommendation": "string (max 280 chars)",
  "cost_usd": "number"
}
```

**AC-3:** Trait `LlmClient` em `insights/llm_client.rs`:
```rust
#[async_trait]
pub trait LlmClient: Send + Sync {
    async fn request_insight_streaming(
        &self,
        context: InsightsContext,
        callback: Arc<dyn InsightsCallback>,
        cancel: Arc<CancelHandle>,
    ) -> Result<InsightsOutput, InsightsError>;
}
```
Com implementações `RealAnthropicClient` e `MockLlmClient`.

**AC-4:** `InsightsContext` struct definida com campos de privacidade (sem api_keys, sem account names — apenas aggregates):
```rust
pub struct InsightsContext {
    pub usage_payload: Vec<VendorAggregate>,  // apenas numérico
    pub since_days: u32,
    pub prompt_version: String,
}
```

**AC-5:** Budget guards implementados em `insights/budget.rs`:
- Input token count estimado antes de enviar; acima de 8000 tokens retorna `InsightsError::InputBudgetExceeded`
- `max_tokens = 1000` no request Anthropic
- Timeout de 15s no request; acima retorna `InsightsError::Timeout`
- Rate limit: 1 request por minuto por instância do client (via `tokio::time::Instant`)

**AC-6:** Streaming callback interface declarada no UDL:
```udl
callback interface InsightsCallback {
    void on_token(string token);
    void on_error(string error);
};
```
> Nota: `on_done` foi removido — o `InsightsOutput` final é entregue como valor de retorno do future `[Async]`, não mais como parâmetro de callback (vide AR-2 resolution §Decision 1).

**AC-7:** `InsightsOutput` struct exposta via UDL como `dictionary`:
```udl
dictionary InsightsOutput {
    string headline;
    sequence<InsightItem> insights;
    string recommendation;
    f64 cost_usd;
    i64 latency_ms;
    string prompt_version;
};
```
> Nota: `InsightsOutput` agora é retornado como valor do future `[Async]`, não mais como parâmetro de callback (vide AR-2 resolution Decision 1).

**AC-8:** Tokio runtime compartilhado via `OnceCell<Runtime>` em `crates/torven-core/src/runtime.rs` com `new_current_thread()` (não multi-thread — ADR-7 nota sobre sandbox macOS). Runtime é `&'static Runtime` via `OnceCell::get_or_init`; nunca chamar `Runtime::shutdown_timeout` do contexto FFI em v1.0 (deferred per AR-2 resolution §Decision 2).

**AC-9:** Testes de integração em `crates/torven-core/tests/insights_e2e.rs` com `MockLlmClient`:
- `test_insights_with_mock()`: chama `request_insight_streaming` com mock, valida callback `on_done` recebido com output válido
- `test_budget_exceeded()`: context com payload acima de 8000 tokens retorna `InsightsError::InputBudgetExceeded`
- `test_privacy_no_api_keys()`: payload de `InsightsContext` não contém api_key (verificar campos)

**AC-10:** FFI surface expõe `CancelHandle` interface (constructor, método `cancel()`, método `is_cancelled() -> bool`) backed por `tokio_util::sync::CancellationToken`. Streaming task usa `tokio::select!` para race entre HTTP chunks e cancellation. Drop do `CancelHandle` no Swift sinaliza cancellation ao Rust.

---

## Tasks / Subtasks

- [x] **T1 — Criar estrutura de diretórios `insights/`**
  ```
  crates/torven-core/src/insights/
  crates/torven-core/src/insights/mod.rs
  crates/torven-core/src/insights/client.rs
  crates/torven-core/src/insights/llm_client.rs
  crates/torven-core/src/insights/schema.rs
  crates/torven-core/src/insights/budget.rs
  crates/torven-core/src/insights/cancel.rs   (NEW per AR-2 §Decision 3)
  crates/torven-core/src/insights/eval.rs     (stub para Story 1.17)
  ```

- [x] **T2 — Criar `runtime.rs` com OnceCell tokio Runtime**
  ```rust
  use once_cell::sync::OnceCell;
  use tokio::runtime::Runtime;

  static RUNTIME: OnceCell<Runtime> = OnceCell::new();

  pub fn get_runtime() -> &'static Runtime {
      RUNTIME.get_or_init(|| {
          tokio::runtime::Builder::new_current_thread()
              .enable_all()
              .build()
              .expect("Failed to create tokio runtime")
      })
  }
  ```
  - Adicionar `tokio-util = { version = "0.7", features = ["rt"] }` em `[dependencies]` de `crates/torven-core/Cargo.toml` (necessário para `CancellationToken` — vide AR-2 resolution §Decision 3)

- [x] **T3 — Definir tipos em `insights/schema.rs`**
  - `InsightsContext`, `VendorAggregate`, `InsightsOutput`, `InsightItem`
  - `InsightItemType` enum: `Trend | Anomaly | BudgetRisk | OptimizationOpportunity`
  - `InsightSeverity` enum: `Info | Warning | Critical`
  - JSON schema validation: `fn validate_output(json: &str) -> Result<InsightsOutput>`

- [x] **T4 — Criar trait `LlmClient` e `MockLlmClient`**
  - Trait com método async (via `async_trait = "0.1"`)
  - `MockLlmClient { response: InsightsOutput, delay_ms: u64, token_chunks: Vec<String> }`
  - Mock chama `on_token` para cada chunk, depois `on_done` com output

- [x] **T5 — Implementar `RealAnthropicClient` em `insights/client.rs`**
  - HTTP POST para `https://api.anthropic.com/v1/messages` com streaming
  - Request body: `model = "claude-3-5-sonnet-20241022"`, `tool_choice = { type: "tool", name: "submit_insight" }`, `stream = true`
  - Tool definition com JSON schema de `prompts/insights.v1.schema.json`
  - Parse `content_block_delta` events com `input_json_delta` — acumular JSON parcial
  - Quando `message_stop`, parse JSON completo, validar schema, retornar `Ok(InsightsOutput)` como valor do future (não mais via `on_done` — vide AR-2 resolution §Decision 1)
  - Privacy: substituir account names por `account_id` hash antes de enviar
  - Implementar cancellation via `tokio::select!` race entre HTTP stream chunks e `cancel_handle.cancelled()`. Ao cancelar, abort imediato + zero callbacks emitidos depois (vide AR-2 resolution §Decision 3).

- [x] **T6 — Implementar budget guards em `insights/budget.rs`**
  - `fn estimate_input_tokens(prompt: &str, context: &InsightsContext) -> u32`
  - `fn check_cost_budget(estimated_tokens: u32, model: &str) -> Result<f64, InsightsError>`
  - Rate limiter simples com `Instant::now()` — erro se chamado dentro de 60s da última chamada

- [x] **T7 — Criar prompts**
  - `prompts/insights.v1.md` com frontmatter + body conforme AC-1
  - `prompts/insights.v1.schema.json` com schema JSON Tool conforme AC-2
  - `prompts/judge.v1.md` (stub — para Story 1.17)
  - `prompts/CHANGELOG.md` (stub)

- [x] **T8 — Expor surface FFI no UDL** (conforme AR-2 resolution §Decision 1)
  - [x] Definir error enum `InsightsError` no UDL
  - [x] Definir `callback interface InsightsCallback` (sem `on_done` — conforme AC-6 atualizado)
  - [x] Definir dictionaries `VendorAggregate`, `InsightsContext`, `InsightItem`, `InsightsOutput` + enums `InsightItemType`, `InsightSeverity`
  - [x] **CLOSED 2026-06-04 — D-1 REVERSED:** `interface CancelHandle` (constructor + `cancel()` + `is_cancelled() -> bool`) and `interface InsightsClient` with `[Async, Throws=InsightsError] request_insight_streaming(InsightsContext, InsightsCallback, CancelHandle) -> InsightsOutput` added to `torven_core.udl`. `uniffi_exports.rs` exposes a thin `InsightsClient` wrapper around `RealAnthropicClient` that bridges `Box<dyn InsightsCallback>` → `Arc<dyn InsightsCallback>` and wraps the inner future in `::uniffi::deps::async_compat::Compat::new(...)` because UDL-mode scaffolding does not pass `async_runtime = "tokio"` to the generated `#[export_for_udl]` attribute. `CancelHandle::new()` returns `Self` (UDL scaffolding wraps in `Arc::new`); a `CancelHandle::new_arc()` helper covers Rust-side callers that need `Arc<CancelHandle>` directly. Build green: `cargo build -p torven-core` generates scaffolding cleanly (`target/debug/build/torven-core-*/out/torven_core.uniffi.rs` contains the expected `struct InsightsClient { }` + 3 `#[::uniffi::export_for_udl] impl InsightsClient` blocks + `struct CancelHandle { }` + 3 export blocks).

- [x] **T9 — Spike empírico AR-2 (5 assertions)**
  - Implementar `crates/torven-core/tests/ar2_async_ffi_spike.rs` conforme skeleton em `docs/architecture/ar-2-spike-resolution.md §Decision 4`
  - Rodar: `cargo test -p torven-core --test ar2_async_ffi_spike -- --ignored --nocapture`
  - 5 assertions PASS obrigatórias antes do merge: p99 cancel <100ms; zero orphan tokio tasks (runtime metrics `alive_tasks` volta a baseline); zero callbacks emitidos pós-cancel; RSS delta <5 MiB em 1000 ciclos; happy-path control group passa
  - Registrar resultados (p99 latency, max RSS delta, alive_tasks baseline) no Change Log da story, mirrorando formato AR-3 da Story 1.5

- [x] **T10 — Escrever testes de integração**
  - `crates/torven-core/tests/insights_e2e.rs` com 3 testes conforme AC-9
  - Adicionar `async-trait = "0.1"` em `[dev-dependencies]`

- [x] **T11 — Atualizar `lib.rs` e `Cargo.toml`**
  - `pub mod insights;`, `pub mod runtime;`
  - Adicionar deps: `reqwest = { version = "0.12", features = ["json", "stream"] }`, `once_cell = "1.19"`, `async-trait = "0.1"`, `tokio-util = { version = "0.7", features = ["rt"] }`, `futures-util = "0.3"`, `sha2 = "0.10"`
  - Adicionar dev-dep: `libc = "0.2"` (para `getrusage` no AR-2 spike)

---

## Dev Notes

### Architecture References

- **ADR-7** (`docs/architecture/torven-v1-adr.md#adr-7`): Anthropic tool_use mode. Streaming flow detalhado (Swift UI → Rust → Anthropic → callback). Tokio `OnceCell` com `new_current_thread()`. Cancellation via `tokio::sync::oneshot`.
- **ADR-4** (`docs/architecture/torven-v1-adr.md#adr-4`): `callback interface InsightsCallback` no UDL. `[Async]` annotation. Memory safety via UniFFI reference counting.
- **AR-2** (`docs/architecture/torven-v1-adr.md#7-architectural-risks`): Tokio runtime em FFI context. Spike de 0.5d obrigatório nesta story para validar cancellation.
- **PRD §6** (`docs/prd/torven-v1.md#6-ai-insights`): Eval pipeline, prompt versioning, cost budgets, privacy payload spec.

### Streaming Flow Completo (do ADR-7)

```
Swift await coreBridge.requestInsights(context, callback: self)
     ↓ UniFFI [Async]
Rust: insights::client::run_streaming(context, callback)
     ↓ tokio runtime (get_runtime().block_on(...))
HTTP POST Anthropic streaming
     ↓ per chunk: callback.on_token(partial_json)
     ↓ message_stop: parse JSON + validate schema
     ↓ callback.on_done(InsightsOutput)
```

### Privacy do Payload (PRD §6.5)

O `InsightsContext.usage_payload` enviado para Anthropic NÃO deve conter:
- `api_key` de nenhum vendor
- Nome de accounts/clientes em plaintext
- Prompts ou outputs reais do usuário

Deve conter APENAS:
- Agregados numéricos por vendor (tokens, custo, janela temporal)
- Tags genéricas (`client`, `personal`, `team`)
- Histogramas horários dos últimos N dias

A `account_id` no payload é `sha256(vendor_id + account_name)[..8]` — determinístico mas não reversível.

### Files Affected

**CRIAR (delivered):**
- `crates/torven-core/src/runtime.rs` — tokio runtime via `OnceCell::get_or_init` with `new_current_thread()` (AR-2 §Decision 2)
- `crates/torven-core/src/insights/mod.rs` — module surface
- `crates/torven-core/src/insights/schema.rs` — `InsightsContext`, `VendorAggregate`, `InsightsOutput`, `InsightItem`, `InsightItemType`, `InsightSeverity`, `InsightsError`, `validate_output`
- `crates/torven-core/src/insights/llm_client.rs` — `InsightsCallback` trait, `LlmClient` async-trait, `MockLlmClient`
- `crates/torven-core/src/insights/client.rs` — `RealAnthropicClient` (HTTP streaming + `tokio::select!` cancellation + SHA-256 privacy redaction)
- `crates/torven-core/src/insights/budget.rs` — `estimate_input_tokens`, `check_cost_budget`, `RateLimiter` (1/min)
- `crates/torven-core/src/insights/cancel.rs` — `CancelHandle` wrapping `tokio_util::sync::CancellationToken` (AR-2 §Decision 3)
- `crates/torven-core/src/insights/eval.rs` — stub for Story 1.17
- `crates/torven-core/tests/insights_e2e.rs` — 3 integration tests with `MockLlmClient` (AC-9)
- `crates/torven-core/tests/ar2_async_ffi_spike.rs` — 3 `#[ignore]`'d tests covering 5 AR-2 assertions (T9)
- `crates/torven-core/tests/support/mod.rs` — shared test helpers entrypoint
- `crates/torven-core/tests/support/mock_anthropic.rs` — hand-rolled `tokio::net` SSE server with paced inter-chunk delay
- `prompts/insights.v1.md` — versioned prompt with frontmatter (AC-1)
- `prompts/insights.v1.schema.json` — JSON Schema for the `submit_insight` tool (AC-2)
- `prompts/judge.v1.md` — Story 1.17 stub
- `prompts/CHANGELOG.md` — prompt change log
- `docs/stories/epics/epic-torven-v1/decision-log-1.15.md` — non-obvious implementation decisions (D-1 through D-4)

**MODIFICAR (delivered):**
- `crates/torven-core/Cargo.toml` — `tokio-util`, `once_cell`, `async-trait`, `reqwest stream`, `futures-util`, `sha2` deps; `libc` dev-dep; `tokio` features extended with `net` + `io-util` for the spike's hand-rolled HTTP listener
- `crates/torven-core/src/lib.rs` — `pub mod insights`, `pub mod runtime`, re-export of insights types at crate root (required by UDL scaffolding lookup)
- `crates/torven-core/src/torven_core.udl` — added `[Error] enum InsightsError`, dictionaries (`VendorAggregate`, `InsightsContext`, `InsightItem`, `InsightsOutput`), enums (`InsightItemType`, `InsightSeverity`), `callback interface InsightsCallback`, `interface CancelHandle` (constructor + `cancel()` + `is_cancelled() -> bool`), and `interface InsightsClient` with `[Async, Throws=InsightsError] request_insight_streaming(InsightsContext, InsightsCallback, CancelHandle) -> InsightsOutput`. T8 fully closed (D-1 REVERSED).
- `crates/torven-core/src/uniffi_exports.rs` — added `pub struct InsightsClient` thin wrapper around `RealAnthropicClient`. Constructor returns `Self` (UDL scaffolding wraps in `Arc::new`). Async method bridges `Box<dyn InsightsCallback>` (UDL ABI) → `Arc<dyn InsightsCallback>` (internal `LlmClient` trait) via `Arc::from(Box<dyn ...>)` (O(1)) and wraps the inner future in `::uniffi::deps::async_compat::Compat::new(...)` to provide a tokio context that UDL-mode scaffolding does not establish on its own.
- `crates/torven-core/src/insights/cancel.rs` — `CancelHandle::new()` now returns `Self` (was `Arc<Self>`) so the UDL constructor scaffolding can wrap with `Arc::new(uniffi_result)`. Added `CancelHandle::new_arc() -> Arc<Self>` for Rust-side callers (`LlmClient::request_insight_streaming` signature, tests, AR-2 spike).
- `crates/torven-core/src/lib.rs` — re-export `CancelHandle` from `crate::insights` and `InsightsClient` from `crate::uniffi_exports` at crate root so the generated scaffolding can resolve `crate::CancelHandle` and `crate::InsightsClient`.
- `crates/torven-core/src/insights/llm_client.rs` — internal test callsites updated from `CancelHandle::new()` (returned `Arc<Self>`) to `CancelHandle::new_arc()`. Semantics unchanged.
- `crates/torven-core/tests/insights_e2e.rs`, `crates/torven-core/tests/ar2_async_ffi_spike.rs` — same `CancelHandle::new() → CancelHandle::new_arc()` rename. AR-2 spike runs identically (cancellation logic untouched).

### Dependências

- Story 1.13 deve estar completa (history layer — AI Insights usa histórico como input)
- Story 1.4 deve estar completa (torven-core lib)
- Story 1.17 (Eval runner) pode rodar em paralelo após esta story

### Testing Strategy

- `MockLlmClient` para todos os testes automáticos — sem chamadas reais à API Anthropic em testes
- `cargo test -p torven-core` com testes de insights
- Manual smoke test com API key real (fora do CI) para validar streaming real

### Definition of Done

- [x] `prompts/insights.v1.md` e schema criados
- [x] Trait `LlmClient` + `RealAnthropicClient` + `MockLlmClient`
- [x] Budget guards funcionais (input tokens, cost ceiling, timeout, rate limit)
- [x] Callback interface no UDL declarada; `[Async] InsightsClient.request_insight_streaming` + `interface CancelHandle` adicionados (T8 fechado 2026-06-04, D-1 REVERSED)
- [x] Spike AR-2 executado: cancellation end-to-end validado (3/3 spike tests GREEN, 5/5 assertions PASS)
- [x] ≥3 testes de integração com MockLlmClient passando (`tests/insights_e2e.rs` → 3 passed)

---

## Risk: AR-2 — Tokio runtime em FFI context

**Severidade:** RESOLVED (design); spike empírico pending @dev (T9).

Resolvido por `docs/architecture/ar-2-spike-resolution.md` (@architect, 2026-06-04). Spike empírico em T9 valida a decisão antes do merge.

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-05-31 | @sm (River) | Story criada — Draft |
| 2026-05-31 | @po (Pax) | Status: Draft → Ready (validation GO, score 8/10) |
| 2026-06-04 | @architect (Aria) → @po (Pax) | AR-2 resolvido via docs/architecture/ar-2-spike-resolution.md; AC-3, AC-6, AC-7, AC-8, T2, T5, T8, T9 atualizados; AC-10 adicionado; Risk section substituída. Story permanece Ready. |
| 2026-06-04 | @dev (Dex) | Status: Ready → InProgress. Branch `feature/wave1-story-1.15-ai-insights-core` criada. T9 (AR-2 spike) será executada primeiro conforme orientação YOLO. |
| 2026-06-04 | @dev (Dex) | Status: InProgress → InReview. **All tasks delivered** (T8 PARTIAL — `[Async]` FFI entry point and `interface CancelHandle`/`InsightsClient` deferred to Story 1.19 per decision-log §D-1; supporting UDL types — error enum, dictionaries, callback interface — fully declared). **AR-2 spike: 5/5 assertions PASS.** p50 cancel latency = 143.8µs, p99 = 433.6µs, max = 3.83ms (well below 100ms ceiling). RSS delta across 1000 cycles = 0 bytes (baseline = 12.53 MiB, final = 12.53 MiB; macOS `getrusage`/`ru_maxrss`). Happy-path 100/100 OK, all emitted ≥1 token. Zero `on_token` callbacks fired after `cancel()` observed across all 1000 cycles. **Test suite: 147 lib + 3 insights_e2e + 18 anthropic_e2e + 3 history_e2e + 4 config + 2 keychain + bin = 0 failures; 3 AR-2 spike tests run separately with `--ignored` (all PASS).** `cargo fmt --check` clean; `cargo clippy --all-targets -- -D warnings` clean (1 `manual_div_ceil` fixed). `cargo machete` not available locally — deferred to CI / @qa. Tech debt logged in `decision-log-1.15.md`: D-1 (UDL `[Async]` deferred), D-2 (chars/4 token heuristic), D-3 (8-hex-char privacy hash), D-4 (`eval.rs` stub). |
| 2026-06-04 | @dev (Dex) | T8 fechado completamente — `[Async] InsightsClient.request_insight_streaming(...)` + `interface CancelHandle` adicionados ao UDL. Decisão D-1 do `decision-log-1.15.md` REVERSED pelo orquestrador (escopo da story permanece dentro de 1.15, não defere para 1.19). `uniffi_exports.rs` agora expõe `pub struct InsightsClient` (thin wrapper around `RealAnthropicClient`) — constructor returns `Self`, async method takes `Box<dyn InsightsCallback>` per UDL ABI and bridges to `Arc<dyn InsightsCallback>` for the internal `LlmClient` trait; future wrapped in `::uniffi::deps::async_compat::Compat::new(...)` to establish a tokio context (UDL-mode scaffolding does not pass `async_runtime = "tokio"` to the generated `#[export_for_udl]` attribute — confirmed via direct read of `uniffi_macros-0.29.5/src/export/scaffolding.rs` lines 280-285 and the absence of `async_runtime` in `uniffi_bindgen-0.29.5/src/scaffolding/templates/ObjectTemplate.rs`). `CancelHandle::new()` now returns `Self` so UDL constructor scaffolding can `Arc::new(uniffi_result)`; `CancelHandle::new_arc()` covers Rust-side `Arc<CancelHandle>` callers. Status remains InReview. **Quality gates re-run:** `cargo fmt --check` clean, `cargo clippy -p torven-core --all-targets -- -D warnings` clean, `cargo test -p torven-core` = 148 lib + 3 anthropic_e2e + 18 config + 4 history_e2e + 3 insights_e2e + 2 keychain = 0 failures. AR-2 spike not re-run — new code is FFI wiring (struct definitions + Arc/Box conversion + `Compat::new` wrap), does not touch the cancellation race that the spike validates. New commit (not amend of ce96b33). |
