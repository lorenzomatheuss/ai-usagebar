# Story 1.2 — Setup UniFFI binding tool

**Story ID:** 1.2.setup-uniffi
**Status:** InReview
**Epic:** Torven v1.0 — Pivot macOS native
**Estimated Effort:** M (1 dia, incluindo spike obrigatório 0.5d)

---

## Story (User Story)

Como desenvolvedor do Torven, eu quero o pipeline UniFFI configurado em `torven-core` com uma função de teste (`ping() -> String`) exposta via UDL e consumida em Swift, para que eu possa validar que a cadeia Rust → FFI → Swift funciona de ponta a ponta antes de expor funções de negócio reais.

---

## Contexto e Motivação

O pivô SwiftUI+Rust exige uma ponte FFI entre o Rust core e o app Swift. A escolha arquitetural é UniFFI (Mozilla) — framework battle-tested em Firefox iOS, Mullvad VPN, Matrix Element iOS.

Esta story executa o **spike obrigatório** descrito em AR-1 do ADR: validar que UniFFI funciona em macOS especificamente (não apenas iOS, que é o target primário dos exemplos oficiais) antes de construir qualquer feature real sobre ele.

**O objetivo desta story é um "hello FFI" funcional:** Swift chama `torvenCore.ping()` e recebe `"pong"`.

**Referência:** `docs/architecture/torven-v1-adr.md#adr-4` (UniFFI — decisão principal do ADR v2).

---

## Acceptance Criteria

**AC-1:** `uniffi = { version = "0.27", features = ["tokio"] }` adicionado em `[dependencies]` de `crates/torven-core/Cargo.toml`.

**AC-2:** `uniffi = { version = "0.27", features = ["build"] }` adicionado em `[build-dependencies]` de `crates/torven-core/Cargo.toml`.

**AC-3:** `crates/torven-core/build.rs` contém `uniffi::generate_scaffolding("src/torven_core.udl").unwrap();`.

**AC-4:** `crates/torven-core/src/torven_core.udl` existe com declaração mínima:
```udl
namespace torven_core {
    string ping();
};
```

**AC-5:** `crates/torven-core/src/uniffi_exports.rs` exporta a função `ping()` com annotation `#[uniffi::export]` (ou equivalente via `uniffi::include_scaffolding!`).

**AC-6:** `cargo build -p torven-core` completa sem erros após adição do UniFFI.

**AC-7:** O script `apple/scripts/build-xcframework.sh` existe (pode ser stub por enquanto com comentários do que fará) e documenta o fluxo de geração do `.xcframework`.

**AC-8:** Documentação inline em `torven_core.udl` explica a convenção de tipos: timestamps como `i64` (Unix), valores monetários como `f64`, erros via `[Throws=ErrorType]`.

---

## Tasks / Subtasks

- [x] **T1 — Adicionar dependências UniFFI em `Cargo.toml` de torven-core**
  - DONE: `uniffi = { version = "0.29", features = ["tokio"] }` em `[dependencies]`.
  - DONE: `uniffi = { version = "0.29", features = ["build"] }` em `[build-dependencies]`.
  - DESVIO da spec: versão **0.29** (não 0.27). Justificativa: spike T6 encontrou
    bug AR-1 em 0.27 (`#[no_mangle]` sem `unsafe(...)` wrapper, incompatível
    com Rust edition 2024 que o workspace usa). 0.29 ships a correção. Esta
    decisão e seu impacto estão no Change Log abaixo.
  - DONE: feature opt-in `uniffi-cli = ["uniffi/cli"]` adicionada e gated ao
    binário `uniffi-bindgen` (`required-features = ["uniffi-cli"]`) para não
    pagar custo de compilação de `clap`/`askama` em builds normais.

- [x] **T2 — Criar `build.rs`**
  - DONE: `crates/torven-core/build.rs` invoca `uniffi::generate_scaffolding("src/torven_core.udl")`.

- [x] **T3 — Criar `src/torven_core.udl` mínimo**
  - DONE: `namespace torven_core { string ping(); };` com docstring sobre a
    finalidade do `ping()` como AR-1 spike validator.

- [x] **T4 — Criar `src/uniffi_exports.rs` com implementação de `ping()`**
  - DONE: módulo UDL mode (não proc-macro). `pub fn ping() -> String { "pong".to_string() }`.
  - DESVIO da spec: a story original mostrava `uniffi::setup_scaffolding!()` +
    `#[uniffi::export]` dentro do módulo. Essa é a sintaxe **proc-macro mode**.
    O ADR-4 recomenda UDL mode, então usamos `uniffi::include_scaffolding!`
    no crate root (vide T5) e função Rust plain neste módulo. AC-5 explicita
    "or equivalent via `uniffi::include_scaffolding!`" — caminho válido.

- [x] **T5 — Atualizar `src/lib.rs` para incluir o módulo**
  - DONE: `pub mod uniffi_exports;` + `pub use uniffi_exports::ping;` +
    `uniffi::include_scaffolding!("torven_core");` no crate root.
  - DESVIO: o macro `include_scaffolding!` precisa estar no crate root, não
    no submódulo, porque o scaffolding gerado declara `UniFfiTag` que os
    `#[uniffi::export_for_udl]` atributos resolvem como `crate::UniFfiTag`.
    Documentado nos doc-comments de `lib.rs` e `uniffi_exports.rs`.
  - O `ping()` stub original (Story 1.1) que retornava `"torven-core"` foi
    removido e substituído pelo `ping() -> "pong"` da Story 1.2. O teste
    `ping_returns_crate_name` foi substituído por `ping_returns_pong`.

- [x] **T6 — Executar spike: gerar Swift bindings e validar** (AR-1 SPIKE)
  - DONE. Resultado documentado em detalhe no Change Log abaixo.
  - **Outcome: UniFFI macOS FUNCIONA (com 0.29, não com 0.27).** Sem fallback
    swift-bridge necessário.

- [x] **T7 — Criar `apple/scripts/build-xcframework.sh` (stub documentado)**
  - DONE: stub criado com fluxo completo de Story 1.5 documentado em
    comentários (uniffi-bindgen generate → cargo build aarch64/x86_64 → lipo
    → xcodebuild -create-xcframework → cópia para `apple/Frameworks/`).
  - DONE: `chmod +x` aplicado.

- [x] **T8 — Documentar convenção de tipos em comentários do UDL**
  - DONE: bloco de comentário extenso no topo de `torven_core.udl` cobre:
    primitivos, timestamps (i64 epoch ms), monetário (f64 USD), opcionais,
    sequence<T>, errors via `[Throws=ErrorType]`, strings UTF-8, async via
    `[Async]`. SECURITY note proíbe expor api_keys/tokens via FFI.

- [x] **T9 — Validar `cargo build --workspace` continua verde**
  - DONE. Saídas:
    - `cargo build --workspace`: Finished in 25.86s (cold) / 2.06s (warm). Zero erros.
    - `cargo clippy --workspace --all-targets -- -D warnings`: clean.
    - `cargo test -p torven-core`: 1 passed (`ping_returns_pong`).
    - `cargo machete`: stub crates (torven-tui, torven-core) ainda listam deps
      não usadas porque os módulos reais não foram migrados (Stories 1.3/1.4).
      Não é regressão desta story; UniFFI é usado.

---

## Dev Notes

### Architecture References

- **ADR-4** (`docs/architecture/torven-v1-adr.md#adr-4`): UniFFI vs swift-rs — UniFFI escolhido por production maturity (Firefox iOS, Mullvad VPN). Decisão de maior impacto do ADR.
- **AR-1** (`docs/architecture/torven-v1-adr.md#7-architectural-risks`): Risco de UniFFI macOS-specific gaps. Esta story é o spike obrigatório.
- **AR-3** (`docs/architecture/torven-v1-adr.md#7-architectural-risks`): Memory ownership em FFI. Mesmo no `ping()` simples, validar que retorno de String não vaza.

### Detalhes do UniFFI 0.27

- **Modo UDL (recomendado):** arquivo `.udl` declarativo + `build.rs` gera scaffolding. Este é o modo desta story.
- **Modo proc-macro (alternativo):** `#[uniffi::export]` direto no Rust sem UDL. O ADR menciona como opção mas recomenda UDL por ser mais battle-tested.
- **Tokio feature:** necessário para suporte a funções `[Async]` no UDL (será crítico na Story 1.15 — AI Insights streaming).
- **Versão 0.27:** suporta `[Async]` annotation para expor `async fn` Rust como `async throws` Swift.

### Files Affected

**CRIAR:**
- `crates/torven-core/build.rs`
- `crates/torven-core/src/torven_core.udl`
- `crates/torven-core/src/uniffi_exports.rs`
- `apple/scripts/build-xcframework.sh`

**MODIFICAR:**
- `crates/torven-core/Cargo.toml` (adicionar deps uniffi)
- `crates/torven-core/src/lib.rs` (adicionar `pub mod uniffi_exports;`)

### Dependências

- Story 1.1 deve estar completa (workspace existindo)
- Pode rodar em paralelo com Story 1.3 (Cleanup) depois que 1.1 completar

### Testing Strategy

- `cargo build -p torven-core` deve compilar sem erros
- `cargo run --bin uniffi-bindgen generate ... --language swift` deve produzir arquivos Swift válidos
- Inspeção manual dos arquivos gerados: `torven_core.swift` deve conter `public func ping() -> String`
- Não há testes automáticos Rust para o ping nesta story — validação é build + generate output

### Definition of Done

- [x] `cargo build --workspace` verde
- [x] `build.rs` + `torven_core.udl` + `uniffi_exports.rs` criados
- [x] `uniffi-bindgen generate` produz `torven_core.swift` com `func ping() -> String`
- [x] Script `build-xcframework.sh` criado (stub documentado)
- [x] Spike documentado no Change Log: output do generate visto

---

## Risk: AR-1 — UniFFI macOS gaps

**Severidade:** MEDIUM

Se `cargo build` com UniFFI falhar em macOS por issue específico (link flags, framework embedding), o fallback documentado no ADR-4 é swift-bridge (~2 dias de retrabalho). Deve ser diagnosticado e documentado aqui se encontrado.

---

## File List

**CREATED (this story):**
- `crates/torven-core/build.rs`
- `crates/torven-core/src/torven_core.udl`
- `crates/torven-core/src/uniffi_exports.rs`
- `crates/torven-core/src/bin/uniffi-bindgen.rs`
- `apple/scripts/build-xcframework.sh` (chmod +x)

**MODIFIED:**
- `crates/torven-core/Cargo.toml` — added `[features] uniffi-cli`, `uniffi = "0.29"` runtime + build dep, `[[bin]] uniffi-bindgen`.
- `crates/torven-core/src/lib.rs` — replaced Story 1.1 ping stub with `pub mod uniffi_exports;` + `pub use uniffi_exports::ping;` + `uniffi::include_scaffolding!("torven_core")` at crate root.

**NOT MODIFIED (in scope but unchanged):**
- `crates/torven-core/src/bin/torven-evals.rs` — pre-existing stub from Story 1.1.

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-05-31 | @sm (River) | Story criada — Draft |
| 2026-05-31 | @po (Pax) | Status: Draft → Ready (validation GO, score 9/10) |
| 2026-05-31 | @dev (Dex) | Status: Ready → InProgress (start of implementation). |
| 2026-05-31 | @dev (Dex) | T1-T5 implementadas; primeiro `cargo build` falhou com `UniFFI 0.27` por `#[no_mangle]` emitido sem `unsafe(no_mangle)` wrapper (incompatível com Rust edition 2024). Decisão: bump para `uniffi = "0.29"` (versão mais recente que corrige). Mudança trazida para todas as deps runtime/build/cli. |
| 2026-05-31 | @dev (Dex) | T6 SPIKE (AR-1 validation): `cargo run --features uniffi-cli --bin uniffi-bindgen -- generate crates/torven-core/src/torven_core.udl --language swift --out-dir /tmp/torven-test-1.2/`. **Output:** 3 arquivos gerados em `/tmp/torven-test-1.2/`: `torven_core.swift` (16606 bytes — contém `public func ping() -> String` na linha 440), `torven_coreFFI.h` (21229 bytes — header C com prototype `uniffi_torven_core_fn_func_ping`), `torven_coreFFI.modulemap` (138 bytes — clang module map `module torven_coreFFI { header "torven_coreFFI.h"; export *; use "Darwin"; ... }`). **Outcome AR-1: UniFFI funciona em macOS com 0.29.** Sem fallback swift-bridge necessário. Bump 0.27 → 0.29 é o único custo. |
| 2026-05-31 | @dev (Dex) | DoD gates: `cargo build --workspace` clean (25.86s cold); `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test -p torven-core` 1 passed (`ping_returns_pong`); `cargo machete` reporta deps não-usadas pré-existentes em stubs (Stories 1.3/1.4 vão migrar módulos reais que as usam). |
| 2026-05-31 | @dev (Dex) | Status: InProgress → InReview. Pronto para @qa. |
