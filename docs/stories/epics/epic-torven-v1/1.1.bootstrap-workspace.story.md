# Story 1.1 — Bootstrap Cargo workspace + Xcode project via XcodeGen

**Story ID:** 1.1.bootstrap-workspace
**Status:** Done
**Epic:** Torven v1.0 — Pivot macOS native
**Estimated Effort:** M (meio dia a 1 dia)

---

## Story (User Story)

Como desenvolvedor do Torven, eu quero um workspace Cargo com múltiplos crates (`torven-core`, `torven-tui`) e um projeto Xcode gerado via XcodeGen (`apple/project.yml`), para que o Rust core e a app SwiftUI possam ser buildados independentemente com um único `make build-all`.

---

## Contexto e Motivação

O codebase atual é um crate flat em `Cargo.toml` com `src/` contendo lib + dois bins. O pivot para macOS native exige:

1. Separar o core Rust em crate lib (`torven-core`) compilável como `staticlib` para link no Xcode
2. Manter o TUI ratatui como crate bin separado (`torven-tui`)
3. Criar estrutura inicial do app Xcode via XcodeGen (YAML declarativo, gitignored `.xcodeproj`)

Esta story entrega o esqueleto que todas as outras stories do Grupo 1 precisam como base.

**Referência:** `docs/architecture/torven-v1-adr.md#adr-1` (Workspace Cargo: multi-crate split) e `docs/architecture/torven-v1-adr.md#adr-9` (XcodeGen).

---

## Acceptance Criteria

**AC-1:** `cargo build --workspace` completa sem erros após a reestruturação.

**AC-2:** `crates/torven-core/` existe como crate lib com `crate-type = ["staticlib", "rlib", "cdylib"]` declarado em `Cargo.toml`.

**AC-3:** `crates/torven-tui/` existe como crate bin com dependência em `torven-core`.

**AC-4:** `apple/project.yml` existe com configuração XcodeGen mínima: target `Torven` macOS 13.0+, `LSUIElement: true`, `MARKETING_VERSION: 1.0.0`.

**AC-5:** `xcodegen generate` executado em `apple/` produz `apple/Torven.xcodeproj/` sem erros (arquivo parseável pelo Xcode).

**AC-6:** `apple/Torven.xcodeproj/` está listado em `.gitignore`; `apple/Torven.xcodeproj/xcshareddata/xcschemes/Torven.xcscheme` está NO gitignore (commitado).

**AC-7:** `Makefile` tem targets `build-core` (cargo build torven-core release) e `build-app` (xcodegen generate + xcodebuild) documentados.

**AC-8:** O crate `crates/torven-evals/` ou `[[bin]] name="torven-evals"` existe como stub com `main.rs` mínimo (`fn main() { println!("torven-evals stub"); }`).

---

## Tasks / Subtasks

- [x] **T1 — Criar workspace root `Cargo.toml`**
  - Substituir `Cargo.toml` atual por workspace manifest: `[workspace] members = ["crates/*"]`
  - Adicionar `resolver = "2"` e campos padrão de workspace (`edition`, `license`, etc.)
  - Mover campos de `[package]` atual para `crates/torven-core/Cargo.toml`

- [x] **T2 — Criar `crates/torven-core/`**
  - Criar estrutura: `crates/torven-core/Cargo.toml`, `crates/torven-core/src/lib.rs`
  - Em `Cargo.toml`: `crate-type = ["staticlib", "rlib", "cdylib"]`
  - Copiar dependências relevantes do `Cargo.toml` atual (reqwest, tokio, serde, etc.)
  - `src/lib.rs` inicial: `pub fn ping() -> String { "torven-core".to_string() }`

- [x] **T3 — Criar `crates/torven-tui/`**
  - Criar `crates/torven-tui/Cargo.toml` como bin com dep `torven-core = { path = "../torven-core" }`
  - `src/main.rs` stub: `fn main() { println!("torven-tui"); }`
  - (O conteúdo real de `src/tui/` será movido na Story 1.4)

- [x] **T4 — Criar stub `torven-evals`**
  - Adicionar `[[bin]] name = "torven-evals" path = "src/bin/torven-evals.rs"` em `crates/torven-core/Cargo.toml`
  - Criar `crates/torven-core/src/bin/torven-evals.rs` com `fn main() { println!("torven-evals stub"); }`

- [x] **T5 — Criar `apple/project.yml`**
  - Conteúdo mínimo (vide ADR-9, §3 do ADR):
    ```yaml
    name: Torven
    options:
      bundleIdPrefix: com.torven
      deploymentTarget:
        macOS: "13.0"
    settings:
      base:
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        DEVELOPMENT_TEAM: "${DEVELOPMENT_TEAM}"
        CODE_SIGN_STYLE: Automatic
    targets:
      Torven:
        type: application
        platform: macOS
        sources:
          - path: Torven
        info:
          path: Torven/Info.plist
          properties:
            LSUIElement: true
            LSApplicationCategoryType: public.app-category.developer-tools
    ```
  - Criar `apple/Torven/TorvenApp.swift` com hello world SwiftUI `@main App`

- [x] **T6 — Criar `apple/Torven/TorvenApp.swift` skeleton**
  ```swift
  import SwiftUI
  @main
  struct TorvenApp: App {
      var body: some Scene {
          MenuBarExtra("Torven", systemImage: "chart.bar.fill") {
              Text("Torven — hello world")
          }
          .menuBarExtraStyle(.window)
      }
  }
  ```

- [x] **T7 — Atualizar `.gitignore`**
  - Adicionar `apple/Torven.xcodeproj/` (exceto xcshareddata/xcschemes/)
  - Adicionar `apple/Frameworks/` (xcframework gerado)
  - Adicionar `apple/Torven/Bridge/Generated/` (UniFFI output)
  - Verificar que `.env` e `*.credentials.json` já estão cobertos

- [x] **T8 — Atualizar `Makefile`**
  - Target `build-core`: `cargo build --release --target aarch64-apple-darwin -p torven-core`
  - Target `build-app`: `cd apple && xcodegen generate && xcodebuild -scheme Torven -configuration Debug`
  - Target `build-all`: depende de `build-core build-app`
  - Target `clean`: `cargo clean && rm -rf apple/Torven.xcodeproj apple/Frameworks`

- [x] **T9 — Verificar `cargo build --workspace` verde**
  - `cargo build --workspace` → exit 0 em 1.48s (torven-core 0.5.0-dev + torven-tui 0.5.0-dev compilados, 0 warnings)
  - `cargo test --workspace` → exit 0 (0 tests run, 0 failures — workspace compila tests sem erros)

- [x] **T10 — Verificar `xcodegen generate` funciona**
  - xcodegen 2.45.4 instalado via `brew install xcodegen`
  - `cd apple && xcodegen generate` → criou `apple/Torven.xcodeproj/project.pbxproj` (9743 bytes)
  - Parseabilidade confirmada empiricamente: usuário abriu `Torven.xcodeproj` no Xcode.app sem erros
  - Nota ambiental: `xcodebuild -list` via CLI falha por `xcode-select` apontar para Command Line Tools (não Xcode.app). Fix sugerido para ambiente dev: `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`. Não bloqueia AC-5 (parseável pelo Xcode = OK).

---

## Dev Notes

### Architecture References

- **ADR-1** (`docs/architecture/torven-v1-adr.md#adr-1`): workspace com 2 crates Cargo (`torven-core` + `torven-tui`) + Xcode project separado em `apple/`. O crate `torven-app` (do ADR v1 Tauri) NÃO existe mais — o app vive no Xcode project.
- **ADR-9** (`docs/architecture/torven-v1-adr.md#adr-9`): XcodeGen YAML → `.xcodeproj`. O `.xcodeproj` é gitignored; o `project.yml` é a fonte da verdade.
- **Workspace Skeleton** (`docs/architecture/torven-v1-adr.md#5-workspace-skeleton-final`): estrutura final de pastas esperada.

### Files Affected

**CRIAR:**
- `Cargo.toml` (workspace root — substitui o atual que era crate único)
- `crates/torven-core/Cargo.toml`
- `crates/torven-core/src/lib.rs`
- `crates/torven-core/src/bin/torven-evals.rs`
- `crates/torven-tui/Cargo.toml`
- `crates/torven-tui/src/main.rs`
- `apple/project.yml`
- `apple/Torven/TorvenApp.swift`

**MODIFICAR:**
- `.gitignore` (adicionar entradas Apple/Xcode)
- `Makefile` (novos targets)

**MANTER INTACTO (por enquanto):**
- `src/` inteiro — será movido na Story 1.3 e 1.4
- Todos os testes existentes — não quebrar

### Dependências

- Nenhuma story anterior (esta é a story INDEP base)
- Requer: `brew install xcodegen` no ambiente de desenvolvimento
- Requer: Xcode instalado (para `xcodebuild -list`)

### Testing Strategy

- `cargo build --workspace` deve retornar exit code 0
- `cargo test --workspace` deve rodar sem erros de compilação (0 testes ok)
- `xcodegen generate` deve rodar sem erros
- `xcodebuild -list -project apple/Torven.xcodeproj` deve listar target "Torven"

### Definition of Done

- [x] `cargo build --workspace` verde (1.48s, 0 warnings)
- [x] `crates/torven-core/` e `crates/torven-tui/` existem com estrutura correta (validado via `cargo metadata --no-deps`)
- [x] `apple/project.yml` válido (YAML lint OK, top keys: name/options/settings/targets)
- [x] `xcodegen generate` produz `.xcodeproj` parseável (3452 byte `Torven.xcscheme` em xcshareddata/xcschemes/; usuário abriu no Xcode.app)
- [x] `.gitignore` atualizado (+10 linhas Apple/Xcode/UniFFI; negação para `xcshareddata/xcschemes/` validada via `git check-ignore -v`)
- [x] `Makefile` com novos targets documentados (build-core, build-app, build-all, clean-apple, help-apple; `make -n help-apple` dry-run OK)

## File List

**Criados (8):**
- `crates/torven-core/Cargo.toml`
- `crates/torven-core/src/lib.rs`
- `crates/torven-core/src/bin/torven-evals.rs`
- `crates/torven-tui/Cargo.toml`
- `crates/torven-tui/src/main.rs`
- `apple/project.yml`
- `apple/Torven/TorvenApp.swift`
- `crates/` e `apple/` diretórios

**Modificados (4):**
- `Cargo.toml` (root convertido para workspace manifest puro — `[workspace]` + `members = ["crates/*"]`, sem `[package]`; `profile.release` preservado)
- `Cargo.lock` (regenerado pelo cargo build com torven-core + torven-tui)
- `.gitignore` (+10 linhas; secrets já cobertos)
- `Makefile` (+5 targets Apple/Rust, sem conflito com targets Linux pré-existentes)

**Gerados (não commitados — gitignored):**
- `apple/Torven.xcodeproj/project.pbxproj`
- `apple/Torven.xcodeproj/project.xcworkspace/` (workspace metadata)
- `target/` (cargo build output)

**Gerados mas committed (via .gitignore negation):**
- `apple/Torven.xcodeproj/xcshareddata/xcschemes/Torven.xcscheme` (3452 bytes; `git check-ignore` confirmou NÃO-ignored)

**Mantidos intactos (órfãos até Story 1.3/1.4):**
- `src/` (todo conteúdo; não membro de nenhum crate do workspace agora)
- `tests/` (anthropic_e2e.rs, live.rs; órfãos no root)

## ACs verificados

| AC | Status | Evidência |
|---|---|---|
| AC-1 cargo build --workspace verde | ✅ | `Finished dev profile in 1.48s`, 0 errors, 0 warnings |
| AC-2 torven-core staticlib+rlib+cdylib | ✅ | `cargo metadata` confirma os 3 crate-types |
| AC-3 torven-tui dep torven-core | ✅ | path-dep resolved; `torven_core::ping()` linka |
| AC-4 apple/project.yml mínimo | ✅ | macOS 13.0, LSUIElement true, MARKETING_VERSION 1.0.0 |
| AC-5 xcodegen generate sem erros | ✅ | `.xcodeproj` parseável (Xcode.app abriu) |
| AC-6 xcscheme committed via gitignore | ✅ | `git check-ignore -v` confirma scheme NÃO-ignored |
| AC-7 Makefile build-core+build-app docs | ✅ | help-apple lista; `make -n` dry-run OK |
| AC-8 torven-evals stub | ✅ | bin compilado dentro de torven-core |

---

## Risks

Nenhum risco arquitetural específico desta story. A story 1.2 e 1.3 podem rodar em paralelo assim que esta completar.

---

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-05-31 | @sm (River) | Story criada — Draft |
| 2026-05-31 | @po (Pax) | Status: Draft → Ready (validation GO, score 9/10) |
| 2026-05-31 | @dev (Dex) | Status: Ready → InProgress (Checkpoint 1: T1-T4 starting) |
| 2026-05-31 | @dev (Dex) | Checkpoint 2 complete: T5 (apple/project.yml) + T6 (TorvenApp.swift) — no build/xcodegen invoked yet (deferred to Checkpoint 4/T10) |
| 2026-05-31 | @dev (Dex) | Checkpoint 3 done: T7 (.gitignore) + T8 (Makefile) |
| 2026-05-31 | @dev (Dex) | Checkpoint 4 partial: xcodegen generate executou (apple/Torven.xcodeproj/ criado); cargo build não rodou (interrompido por quota cutoff) |
| 2026-05-31 | @aiox-master (Orion) | Close-out Story 1.1: cargo build --workspace verde (1.48s); cargo test --workspace OK; .gitignore negation pattern fix (de `apple/Torven.xcodeproj/` para `apple/Torven.xcodeproj/*` + 3 negações encadeadas) para AC-6; DoD 6/6 ✅; File List + ACs verificados adicionados; Status: InProgress → InReview |
| 2026-05-31 | @qa (Quinn) | QA Gate PASS — 7/7 checks OK, 8/8 ACs verified empirically, clippy --workspace --all-targets -D warnings clean, cargo machete skipped (não instalado, INFO); Status: InReview → Done |

---

## QA Results

### Review Date: 2026-05-31

### Reviewed By: Quinn (Guardian / Test Architect)

### Veredito: **PASS**

### 7 Quality Checks

| # | Check | Status | Notas |
|---|-------|--------|-------|
| 1 | Code review | PASS | Manifestos workspace/crates corretos; `crate-type` triplo declarado; deps copiadas sem regressão; `profile.release` preservado em root |
| 2 | Unit tests | PASS | `cargo test --workspace` exit 0 (1 test em torven-core::ping_returns_crate_name; 0 falhas); 0-tests aceitável p/ foundation |
| 3 | Acceptance criteria | PASS | 8/8 ACs verificados empiricamente (build, metadata, gitignore, makefile dry-run, runtime stub) |
| 4 | No regressions | PASS | `src/`, `tests/`, LICENSE, CHANGELOG, CLAUDE.md intactos; 8 targets Linux do Makefile preservados |
| 5 | Performance | N/A | Foundation story, sem código runtime crítico. Baseline: cargo build dev = 1.29s |
| 6 | Security | PASS | Secret patterns todos presentes no .gitignore; zero secrets em código novo; UniFFI Bridge/Generated/ já gitignored |
| 7 | Documentation | PASS | File List completo; Change Log completo; QA Results adicionado (este bloco); README update fora do escopo desta story |

### CLAUDE.md Release Gate

- `cargo clippy --workspace --all-targets -- -D warnings`: **PASS** (exit 0)
- `cargo machete`: **SKIPPED** (não instalado) — INFO: instalar com `cargo install cargo-machete` para próxima story
- `cargo test`: **PASS** (1/1)

### Code Review Detalhado

- `Cargo.toml` root: workspace manifest puro (`[workspace]` + `members = ["crates/*"]` + `[workspace.package]`), `profile.release` (lto/codegen-units/strip) preservado. OK.
- `crates/torven-core/Cargo.toml`: `[lib].name = "torven_core"` (válido — snake_case obrigatório para staticlib link no Xcode); `crate-type = ["staticlib","rlib","cdylib"]` correto; deps reqwest/tokio/serde/toml/clap/chrono/etc copiadas sem regressão; `[[bin]] torven-evals` declarado.
- `crates/torven-core/src/lib.rs`: documentação inline boa (cita Story 1.3/1.4 de migração); inclui test unit pequeno (`ping_returns_crate_name`) — bonus além do mínimo exigido.
- `crates/torven-core/src/bin/torven-evals.rs`: stub mínimo + docstring referenciando track AI Insights. OK.
- `crates/torven-tui/Cargo.toml`: ratatui 0.29 + crossterm 0.28 antecipados (escopo Story 1.4) — aprovado pelo PO; path-dep `torven-core = { path = "../torven-core" }` correto.
- `crates/torven-tui/src/main.rs`: smoke-wire chamando `torven_core::ping()` — defensável e útil p/ validar link no workspace.
- `apple/project.yml`: top-keys [name, options, settings, targets] válidos; macOS 13.0; LSUIElement true; MARKETING_VERSION 1.0.0; LSApplicationCategoryType developer-tools (bonus correto). OK.
- `apple/Torven/TorvenApp.swift`: MenuBarExtra hello world (AC-4 esqueleto cumprido). OK.
- `Makefile`: 5 novos targets (`build-core`, `build-app`, `build-all`, `clean-apple`, `help-apple`) sem conflito com 8 targets Linux pré-existentes; `make -n` dry-run verde em todos.
- `.gitignore`: padrão de negação file-glob (`apple/Torven.xcodeproj/*` + 3 negações encadeadas para `xcshareddata/xcschemes`) bem documentado em comentário (linhas 61-62); `git check-ignore -v` confirma comportamento desejado.

### Validação Empírica dos ACs

| AC | Comando | Resultado |
|----|---------|-----------|
| AC-1 | `cargo build --workspace` | `Finished dev profile [unoptimized + debuginfo] target(s) in 1.29s` (0 warn) |
| AC-2 | `cargo metadata --no-deps` | torven_core: `[staticlib, rlib, cdylib]` confirmado |
| AC-3 | grep + build | path-dep resolvido; `torven_core::ping()` linka em torven-tui |
| AC-4 | ruby YAML parse | macOS=13.0, LSUIElement=true, MARKETING_VERSION=1.0.0, platform=macOS |
| AC-5 | `ls apple/Torven.xcodeproj/` | project.pbxproj=9743 bytes, Torven.xcscheme=3452 bytes |
| AC-6 | `git check-ignore -v` | pbxproj ignored por linha 63; xcscheme NÃO ignored (exit=1) |
| AC-7 | `make -n build-core build-app build-all clean-apple` | dry-run de todos verde |
| AC-8 | `./target/debug/torven-evals` | output: `torven-evals stub` |

### No Regressions

- `src/` (24 entries: active.rs, anthropic/, bin/, cache.rs, config.rs, countdown.rs, error.rs, format.rs, gemini/, lib.rs, ...) intacto — será migrado em Story 1.3/1.4
- `tests/` (anthropic_e2e.rs, fixtures/, live.rs, snapshots/) intacto
- `git diff --stat -- LICENSE CHANGELOG.md CLAUDE.md` = vazio (nada modificado)
- 8 targets Linux do Makefile preservados: build, install, uninstall, test, smoke, clippy, fmt, clean
- Nota informativa: `tests/anthropic_e2e.rs` e `tests/live.rs` no root agora estão **órfãos** (não pertencem a nenhum crate do workspace). CLAUDE.md cita "200+ tests must pass" como gate de release — esses testes voltam ao escopo do workspace na Story 1.3/1.4. **Não bloqueia 1.1** porque a story explicitamente diz "manter intacto por enquanto".

### Security

- `.gitignore` cobre todos os patterns críticos: `.env`, `.env.local`, `.env.*.local`, `*.credentials.json`, `.claude/`, `*.key`, `*.pem`, `.claude/settings.local.json`
- Scan `grep -rEi "(api_key|token|secret|password)\s*[:=]\s*['\"]" crates/ apple/` → zero matches relevantes (apenas `LSApplicationCategoryType` e `DEVELOPMENT_TEAM` que são identifiers Apple, não secrets)
- `apple/Torven/Bridge/Generated/` (UniFFI output futuro) já gitignored na linha 70 — superfície futura coberta
- Surface de ataque desta story: minimal (foundation; sem código de produção runtime)

### Issues Encontradas

| # | Severidade | Categoria | Descrição | Recomendação |
|---|------------|-----------|-----------|--------------|
| 1 | low | tooling | `cargo machete` não instalado no host | Instalar com `cargo install cargo-machete` antes da Story 1.3 (que vai mover dependências) |
| 2 | low | docs | CLAUDE.md release checklist cita "200+ tests must pass" mas os testes em `tests/` estão órfãos do workspace agora | Será resolvido na Story 1.3/1.4 quando `src/` migrar para crates. Adicionar nota no `Dev Notes` da Story 1.3 |

Nenhuma issue medium/high encontrada.

### Gate Status

Gate: **PASS** → `docs/qa/gates/1.1.bootstrap-workspace.yml`

### Next Step

@aiox-master delega @devops para `*push` (git add + commit + push branch + PR).

